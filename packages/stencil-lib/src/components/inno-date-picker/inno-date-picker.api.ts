/**
 * Models the details of a date change.
 */
export type DateChange = {
  /**
   * Start time value.
   * The format is the same as the set value for the component.
   */
  from: string;

  /**
   * End time value.
   * The format is the same as the set value for the component.
   */
  to: string;
};
